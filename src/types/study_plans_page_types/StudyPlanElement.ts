// This is in its own file because it's used in multiple places
export interface StudyPlanElement {
	faculty: string;
	major: string;
	year: string;
	language: string;
	checked: boolean;
	fileKey: string;
	key: string;
}
